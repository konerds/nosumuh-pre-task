import { tw } from '@utils/tw';

const className = {
  DivContainer:
    'w-full flex items-center gap-x-4 mb-4 rounded-md bg-[#f4f4f4] p-4',
};

export const DivContainer = tw('div', className.DivContainer);
